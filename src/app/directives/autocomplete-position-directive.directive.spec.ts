import { AutocompletePositionDirectiveDirective } from './autocomplete-position-directive.directive';

describe('AutocompletePositionDirectiveDirective', () => {
  it('should create an instance', () => {
    const directive = new AutocompletePositionDirectiveDirective();
    expect(directive).toBeTruthy();
  });
});
